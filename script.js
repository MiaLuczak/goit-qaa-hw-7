// Exercise 1
const categories = document.querySelectorAll('ul#categories li.item');
// const categories = [];
// getCategories.forEach((element) =>
//   categories.push(element.querySelectorAll('.item'))
// );
console.log(categories);
